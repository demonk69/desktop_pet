# Services

Reserved integration boundaries: `wifi`, `network`, `time`, `weather`, `ai`, `pc_link`,
and `storage`. V0.1 intentionally contains no service implementations. Services will
publish events and must not call Pet Core internals directly.
